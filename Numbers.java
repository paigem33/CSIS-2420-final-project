import java.util.List;

public class Numbers {

    private List<List<Integer>> cardNumbers;

    // returns the numbers for the cards

    // maybe has a function that calls itself recursively, passing the next range of numbers until it reaches a range that should not be done

}
